\documentclass{ximera}
\input{../../preamble.tex}

\author{Zack Reed}
\begin{document}

%matrix_symmetry
\begin{exercise}
  Consider the matrices
  \begin{equation*}
    A = \startmat{cc}
      1 & 2 \\
      3 & 2 \\
      1 & -1
    \stopmat,
    \quad
    B = \startmat{ccc}
      2 & -5 & 2 \\
      -3 & 2 & 1
    \stopmat
  \end{equation*}
  Find the following if possible.
  \begin{enumerate}
  \item $-3A{^T}$.
  \item $3B - A^T$.
  \end{enumerate}

  $-3A^T$ $\answer[format=string]{is}$ possible because

  \begin{problem}
    the transpose of a matrix is another matrix, and multiplying a matrix by a scalar is possible.


$\startmat{ccc}
        \answer{-3} & -9 & \answer{-3} \\
        -6 & \answer{-6} & 3
      \stopmat$.
\end{problem}

(NOTE: answer with either "is" or "is not" exactly)
$3B - A^T$ $\answer[format=string]{is}$ possible because

  \begin{problem}
\begin{selectAll}
    \choice[correct]{the transpose of a matrix is another matrix, and multiplying a matrix by a scalar is possible.}
    \choice[correct]{the dimensions of $A^T$ and $B$ are the same.}
    \choice{the dimensions of $A^T$ and $B$ are different.}
    \choice{the dimensions of $A^T$ and $3B$ are different.}
    \choice{the dimensions of $A^T$ and $3B$ are the different.}
\end{selectAll}

$\startmat{ccc}
        \answer{5} & -18 & \answer{5} \\
        -11 & \answer{4} & 4
      \stopmat$.
      \end{problem}



  Which of the following matrices are symmetric, antisymmetric, both,
  or neither?

  \begin{equation*}
    A = \startmat{cc}
      0 & 1 \\
      -1 & 0
    \stopmat,
    \quad
    B = \startmat{cc}
      2 & 1 \\
      1 & 3
    \stopmat,
    \quad
    C = \startmat{cc}
      1 & 2 \\
      -2 & 0
    \stopmat,
    \quad
    D = \startmat{cc}
      0 & 0 \\
      0 & 0
    \stopmat.
  \end{equation*}
  (NOTE: answer with either "symmetric", "antisymmetric", "both", or "neither" exactly)

  \begin{hint}
  
    Check the %section for definitions

  \end{hint}
  
  $A$ is $\answer[format=string]{antisymmetric}$, $B$ is $\answer[format=string]{symmetric}$, $C$ is $\answer[format=string]{neither}$, and $D$ is $\answer[format=string]{both}$.
\end{exercise}


\end{document}